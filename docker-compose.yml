services:
  api-server:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env # .env 파일에서 환경 변수 불러오기
    environment:
      - NODE_ENV=development